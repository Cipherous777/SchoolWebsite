<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback Admin - IIS Portal</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: #f5f5f5; }
        .container { max-width: 1200px; margin: 0 auto; background: white; padding: 20px; border-radius: 10px; }
        h1 { color: #2E66D8; text-align: center; }
        .feedback-item { border: 1px solid #ddd; padding: 15px; margin: 10px 0; border-radius: 8px; }
        .feedback-text { font-size: 16px; line-height: 1.5; margin-bottom: 10px; }
        .feedback-meta { font-size: 12px; color: #666; }
        .no-feedback { text-align: center; color: #666; padding: 40px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>User Feedback</h1>
        <% if (feedbackList.length === 0) { %>
            <div class="no-feedback">No feedback submitted yet.</div>
        <% } else { %>
            <% feedbackList.forEach(feedback => { %>
                <div class="feedback-item">
                    <div class="feedback-text"><%= feedback.feedback %></div>
                    <div class="feedback-meta">
                        Submitted: <%= feedback.submittedAt %> | IP: <%= feedback.ip %>
                    </div>
                </div>
            <% }); %>
        <% } %>
    </div>
</body>
</html>